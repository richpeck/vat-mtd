<!DOCTYPE html>
<html lang="en-GB">
  <head>

    {% comment %} General {% endcomment %}
    {% comment %} HTML that should be present on all pages {% endcomment %}
    <title>{{ app.title }}</title>

    {% comment %} Meta Tags {% endcomment %}
    {% comment %} Outlines exactly what should be present on the page {% endcomment %}

    {{ 'app' | javascript_tag }}
    {{ 'app' | stylesheet_tag }}

  </head>

  <body class="">
    <header>
      {% include 'auth' %}
      <h1>{{ "VAT MTD" | link_to: "/" }}</h1>
    </header>
    <main>
      {% if flash %}{% include 'flash' %}{% endif %}
      <div class="container">{{ content }}</div>
    </main>
  </body>

<html>
